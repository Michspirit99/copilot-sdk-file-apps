name: Validate Samples

on:
  workflow_dispatch: # Manual trigger only
  push:
    branches: [ master ]
    paths:
      - 'samples/**'
      - '.github/workflows/**'
  pull_request:
    branches: [ master ]
    paths:
      - 'samples/**'

jobs:
  compile-samples:
    name: Compile All Samples
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Setup .NET 10
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'
        dotnet-quality: 'preview'
    
    - name: Verify .NET version
      run: dotnet --version
    
    - name: Compile all samples
      run: |
        echo "🔨 Compiling all sample files..."
        errors=0
        for file in samples/*.cs; do
          echo "Checking: $file"
          # Remove --no-restore to allow NuGet package downloads
          if dotnet build "$file" 2>&1 | grep -i "error"; then
            echo "❌ Compilation failed: $file"
            errors=$((errors + 1))
          else
            echo "✅ Compiled: $file"
          fi
        done
        
        if [ $errors -gt 0 ]; then
          echo "❌ $errors sample(s) failed to compile"
          exit 1
        fi
        
        echo "✅ All samples compiled successfully!"
  
  test-basic-sample:
    name: Verify Sample Structure
    runs-on: ubuntu-latest
    needs: compile-samples
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Setup .NET 10
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'
        dotnet-quality: 'preview'
    
    - name: Test sample can be analyzed
      run: |
        echo "🔍 Testing sample file structure..."
        
        # Verify samples have proper directives
        for file in samples/*.cs; do
          echo "Analyzing: $file"
          
          # Check for required package directive
          if ! grep -q "#:package" "$file"; then
            echo "⚠️  Warning: $file may be missing package directives"
          fi
          
          # Check for using statements
          if grep -q "using GitHub.Copilot.SDK" "$file"; then
            echo "✅ Uses Copilot SDK: $file"
          fi
        done
        
        echo "✅ Sample structure validation complete!"
    
    - name: Verify package versions
      run: |
        echo "📦 Checking package version specifiers..."
        
        # Ensure all packages have version specifiers
        for file in samples/*.cs; do
          if grep "#:package" "$file" | grep -v "@"; then
            echo "❌ Missing version specifier in $file"
            exit 1
          fi
        done
        
        echo "✅ All packages have version specifiers!"
  
  validate-readme:
    name: Validate Documentation
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Check all samples are documented
      run: |
        echo "📚 Checking README documentation..."
        
        # Get all sample files
        samples=$(ls samples/*.cs | xargs -n 1 basename)
        
        echo "Sample files found:"
        echo "$samples"
        echo ""
        
        missing=0
        for sample in $samples; do
          if ! grep -q "$sample" README.md; then
            echo "❌ Sample not documented in README: $sample"
            missing=$((missing + 1))
          else
            echo "✅ Documented: $sample"
          fi
        done
        
        if [ $missing -gt 0 ]; then
          echo ""
          echo "⚠️  $missing sample(s) missing from README"
          echo "Please add documentation for all samples"
          exit 1
        fi
        
        echo ""
        echo "✅ All samples are documented in README!"
    
    - name: Verify sample count matches
      run: |
        sample_count=$(ls samples/*.cs | wc -l)
        readme_sample_count=$(grep -c "samples/.*\.cs" README.md || true)
        
        echo "Sample files: $sample_count"
        echo "README references: $readme_sample_count"
        
        if [ "$readme_sample_count" -lt "$sample_count" ]; then
          echo "⚠️  README may be missing some samples"
        fi
